let keypress,override,Game,slideRight;_cd3‍.w("keypress",[["default",["keypress"],function(v){keypress=v}]]);_cd3‍.w("prompts",[["override",["override"],function(v){override=v}]]);_cd3‍.w("./engine/game",[["default",["Game"],function(v){Game=v}],["slideRight",["slideRight"],function(v){slideRight=v}]]);




keypress(process.stdin);

/**
 * The code in this file is used to run your game in the console. Use it
 * to help develop your game engine.
 *
 */

let game = new Game(4);
_cd3‍.g.console.log(game.toString());


game.onMove(gameState => {
    _cd3‍.g.console.log(game.toString());
    // console.log(game.gameState);
});

game.onWin(gameState => {
    _cd3‍.g.console.log('You won with a gameState of...', gameState)
});

game.onLose(gameState => {
    _cd3‍.g.console.log('You lost! :(', gameState)
    console.log(`Your score was ${gameState.score}`);
});

process.stdin.on('keypress', function (ch, key) {
    switch (key.name) {
        case 'right':
            game.move('right');
            break;
        case 'left':
              game.move('left');
            break;
        case 'down':
            game.move('down');
            break;
        case 'up':
        game.move('up');
            break;
    }
    if (key && key.ctrl && key.name == 'c') {
        process.stdin.pause();
    }
});


process.stdin.setRawMode(true);
process.stdin.resume();
